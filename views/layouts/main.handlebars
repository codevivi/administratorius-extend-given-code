<html lang="en">

  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Paprastas blog'as</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" />
    <style>
      img { max-width: 100%; }
    </style>
  </head>

  <body>
    <header class="py-3">
      <div class="d-flex justify-content-center">
        <ul class="nav nav-pills">
          <li class="nav-item"><a href="/" class="nav-link active" aria-current="page">Titulinis</a></li>
          {{#if user}}
            <li class="nav-item"><a href="/new-user" class="nav-link">Naujas vartotojas</a></li>
            <li class="nav-item"><a href="/new-photo" class="nav-link">Nauja konkurso nuotrauka</a></li>
            <li class="nav-item"><a href="/gallery" class="nav-link">Konkursas</a></li>
            <li class="nav-item"><a href="/logout" class="nav-link">Atsijungti</a></li>
          {{/if}}
        </ul>
      </div>
      {{#if user}}
        <div class="text-center pt-3">Sveiki, <strong>{{user.name}} {{user.last_name}}</strong></div>
      {{/if}}
    </header>
    <div class="container">
      {{{body}}}
    </div>
  </body>

</html>